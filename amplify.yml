version: 1
frontend:
  phases:
    build:
      commands:
        - npm ci
        - npm run build
    postBuild:
      commands:
        - chmod 777 .
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'